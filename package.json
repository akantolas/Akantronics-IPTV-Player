{
  "name": "tv",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=@tv/xtream-core",
    "test": "npm run test --workspace=@tv/xtream-core",
    "typecheck": "npm run typecheck --workspace=@tv/xtream-core"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
